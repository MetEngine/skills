# Retired

This file has been replaced by `index.md` at the skills root.

The new router uses YAML frontmatter with `triggers` and `requires` for LLM-native routing instead of manual load sequences.

Load `index.md` to route to the correct file.
