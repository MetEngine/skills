version: 1
interface:
  display_name: MetEngine Data Agent
  short_description: Smart money analytics for Polymarket, Hyperliquid, and Meteora. Pay per request with USDC via MPP (Tempo EVM, Solana) or x402 (Solana). No API keys.
  default_prompt: Analyze wallet, market, perp, and LP flow using MetEngine API. Fetch only the required platform docs, then call the relevant endpoint with MPP (Tempo EVM or Solana) or x402 (Solana) payment signing.
